services:
  legal-rag:
    build: .
    container_name: legal-rag-dev
    volumes:
      - .:/app
      - model-cache:/root/.cache    # Cache HuggingFace models
    working_dir: /app
    env_file:
      - .env
    ports:
      - "8000:8000"

volumes:
  model-cache:
